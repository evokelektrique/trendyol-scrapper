version: '3'
services:
  node_app:
    build:
      context: .  # Set to the directory containing your Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ./.env:/usr/src/app/.env
# volumes:
#   trendyol_data:
